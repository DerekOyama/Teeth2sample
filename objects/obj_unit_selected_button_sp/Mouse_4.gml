//you can remove the selection on a given index by just clicking on that button
if(showing_items)
{
	global.spovid.saved_items[attachednumber] = -1;
}
else
{
	global.unit_selection[attachednumber] = -1;
}

